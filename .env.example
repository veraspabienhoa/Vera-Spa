# Vera Spa PostgreSQL (development)
# Copy to the server environment. Never commit the real DB_PASS or DATABASE_URL.
VERA_DB_ENABLED=1
VERA_DATA_BACKEND=postgres
DB_HOST=160.236.192.51
DB_PORT=5432
DB_NAME=veraspa
DB_USER=vera_dev
DB_PASS=<SET_IN_SERVER_SECRET>
DB_SSLMODE=require

# Optional alternative. URL-encode special characters in the password.
# DATABASE_URL=postgresql+psycopg://vera_dev:<PASSWORD>@160.236.192.51:5432/veraspa?sslmode=require
